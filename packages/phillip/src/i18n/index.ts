// The React-free entry (`@nutz/phillip/i18n`): the language a lead is spoken to,
// and every string they read that the model didn't write. Imported by both the
// widget and the dashboard server, so the two can never drift.
export * from "./language";
export * from "./copy";
